// The Match — Apple Watch app (Phase W0).
// Design law (The-Match-AppleWatch-Strategy.docx): the watch is a microphone
// with a haptic engine, not a small screen. W0 ships the skeleton: standalone
// login → profile. W1 adds the hole card + crown scoring; W2 the voice tier.

import SwiftUI

@main
struct TheMatchWatchApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
